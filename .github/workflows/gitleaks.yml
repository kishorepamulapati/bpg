name: GitLeaks

on:
  push:
    branches:
      - main

jobs:
  gitleaks:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.17
          
      - name: Install GitLeaks
        run: go install github.com/zricethezav/gitleaks/v8@latest

      - name: Run GitLeaks
        run: gitleaks --config=".github/.gitleaks.toml"
